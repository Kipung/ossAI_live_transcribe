# Core numerical / ML
numpy==1.26.4
torch==2.9.1

# Whisper (fast + slow models)
openai-whisper==20250625
faster-whisper==1.0.3
ctranslate2==4.6.0

# Audio capture / VAD / offline ASR
pyaudio==0.2.14
webrtcvad==2.0.10
vosk==0.3.44

# Diarization (speaker embeddings)
Resemblyzer==0.1.4
soundfile==0.13.1

# Utility / logging / progress
coloredlogs==15.0.1
tqdm==4.67.1
requests==2.32.5
httpx==0.28.1
huggingface-hub==1.0.1

# Tokenization helpers (used by faster-whisper / LLM side utilities)
tiktoken==0.12.0
tokenizers==0.22.1